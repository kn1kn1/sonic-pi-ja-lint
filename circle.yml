# dependencies:
#   override:
#     - git submodule foreach 'git checkout master; git pull'
# checkout:
#   post:
#     - git submodule foreach 'git checkout master; git pull'

test:
  override:
    - ./exec-lint.sh

  post:
    - mkdir -p $CIRCLE_TEST_REPORTS/junit/
    - cp -p report.xml $CIRCLE_TEST_REPORTS/junit/;

# machine:
#   environment:
